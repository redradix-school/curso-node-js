$(function() {

  // ---8<--- El cliente del chat aqui! ---8<---

  /*

  Para obtener los datos que hemos introducido en login, podemos
  hacer un GET a /me:
  $.get('/me').then(function(userData){
    ....
  })

  Tenemos disponible de forma global el objeto Chat

  Chat.registerHandler(cb)
    Registra un callback que será llamado con el texto del mensaje
    introducido por el usuario

  Chat.postMsg(fromObj, messageObj)
    Añade a la lista de mensajes un mensaje de otro usuario
    fromObj = { name, avatar }
    messageObj = { message, date (timestamp)}

  Chat.showMyMsg(fromObj, messageObj)
    Añade a la lista de mensajes un mensaje propio
  */

});
